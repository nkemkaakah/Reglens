-- RegLens — Nexus Bank seed data
-- Runs on first Postgres init via docker-compose (docker-entrypoint-initdb.d).
-- Expand in later steps when obligation/catalog schemas exist.

-- Example placeholder (safe no-op until tables exist):
-- INSERT INTO ...

SELECT 1 AS reglens_seed_ready;
