# Test profile defaults — mirror main app keys so SecurityFilterChain and WebConfig wire in @SpringBootTest.
app.security.service-token=test-service-token
app.cors.allowed-origins=http://localhost:5173
