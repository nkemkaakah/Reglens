# Test profile — mirrors main keys; Testcontainers @ServiceConnection wires datasource + Mongo URIs.
spring.application.name=ai-registry-service
app.security.service-token=test-service-token
app.cors.allowed-origins=http://localhost:5173
